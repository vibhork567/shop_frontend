import React from "react";
import { ToastContainer, toast } from 'react-toastify';
import { Button } from 'bootstrap';

function Header() {
    return(
        <div style={{background:"indigo" , padding: 20 , color: "white", textAlign: "center"}}>
            <h1>ManageUrShop</h1>
        </div>
     
    )
}

export default Header;